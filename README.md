# File-Packer-Unpacker-with-Encryption
A Java-based Packer-Unpacker application that combines multiple files into a single packed file and extracts them back to their original form. This project demonstrates file handling, stream management, and object-oriented programming concepts in Java.
